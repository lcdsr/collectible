import { CommandMap, Namespace } from '@ionic/cli-framework';
export declare class IonicNgNamespace extends Namespace {
    getMetadata(): Promise<{
        name: string;
        summary: string;
    }>;
    getCommands(): Promise<CommandMap>;
}
export declare function run(argv: string[], env: {
    [key: string]: string;
}): Promise<void>;
